std_dev_list = [1,2,5];
FWHM_list = [0,6,12];
nsubj = 20;

I = 0;
for std_dev = std_dev_list
    I = I + 1;
    J = 0;
    for FWHM = FWHM_list
        J = J + 1;
        out = dispres_sims(std_dev, FWHM, nsubj);
        MSE.is(I,J) = 
    end
end
